---
description: 功能/用法变更必须同步更新用户文档并（发版时）部署官网手册
alwaysApply: true
---

# 文档与用法同步（强制）

凡改动会影响用户使用方式的内容，**同一任务内**必须同步更新文档，不得只改代码。

## 什么算「用法变更」

- 新增/改名/删除 CLI 子命令或参数（如 `iai upgrade`、`iai node role`）
- 新增/变更 HTTP API 或控制台操作流程
- 安装、升级、配置、角色权限、任务生命周期等行为变化
- 发版版本号变更（文档中的示例版本须对齐最新 Release）

## 必须更新的文档面

按影响面至少更新相关项：

| 面 | 路径 | 何时 |
|----|------|------|
| 官网手册 | `web/docs/index.html` | 用户可操作的流程/命令变更时**必更** |
| 快速开始 | `START.md` | 安装/启动/升级入口变更时 |
| 使用说明 | `USAGE.md` | CLI/日常用法变更时 |
| 规格索引 | `specs/STATUS.md` 或当前 feature 规格 | 产品行为变更时（按既有 Speckit 习惯） |

控制台文案若承载关键操作指引，一并改 `web/console/`。

## 完成定义

- 文档中的命令可复制执行，且与当前实现一致（含版本示例，如 `v0.4.3`）
- 若改了 `web/docs/`：任务收尾时运行 `scripts/deploy-docs.sh` 部署到 https://iaiaiai.ai/docs/（除非用户明确说不部署）
- 发版（`scripts/publish.sh` / Release）时：手册示例版本与 Release tag 一致

## 反例（禁止）

- 只实现功能、不写怎么用
- 手册仍写旧流程（如「领取」而实际是「申请加入」）
- 示例版本长期落后于最新 Release
