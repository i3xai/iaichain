//! 核心调度层（IAI Core Layer）。
//!
//! 阶段 0：占位骨架。后续阶段在此落地任务七态生命周期、分解、匹配、聚合与质量门禁。
//! 参见 `specs/001-task-orchestration/plan.md` 与 `DEVELOPMENT-PLAN.md` 阶段 5。

/// crate 版本（与 workspace 对齐）。
pub const VERSION: &str = env!("CARGO_PKG_VERSION");
